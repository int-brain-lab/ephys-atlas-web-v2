/** Stable compatibility facade for the decomposed regional UI subsystem. */
export {
  RegionalPanelController,
  type RegionalPanelCallbacks,
  type RegionalPanelModel,
} from './regional/controller.js';
