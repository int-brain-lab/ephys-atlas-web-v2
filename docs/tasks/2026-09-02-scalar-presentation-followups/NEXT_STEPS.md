# Next steps

Implement one bounded task at a time from `README.md`:

1. Hide only `outside` volume tooltips and retain diagnostic states.
2. Add `std` as an ordinary regional display statistic.
3. Add and classify the five new palettes.
4. Implement Auto versus explicit preferred-palette resolution.
5. Add synthetic-only explicit diverging-center contract and normalization.
6. Replace region fill bars with shared-domain dots.
7. Stop: do not assign real feature palettes/centers or implement volume
   regional distributions without their future decisions.

For every task, inspect the named seams and existing tests first, run focused
tests during development, run `just check`, update `STATUS.md`, and leave one
coherent green commit.
