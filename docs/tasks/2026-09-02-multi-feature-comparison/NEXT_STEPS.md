# Next steps

As the current immediate priority, with the catalog/edition machinery,
scalar-presentation follow-ups, and documentation screenshots complete:

1. add deterministic synthetic spatial/profile adapters and the missing,
   zero-variance, slow, failed, and compatible-different-grid scenarios;
2. build separable Focus, virtualized Gallery, and Profile components around
   the bounded comparison session in a development-only lab;
3. run the first task-based scientist review and record accepted UX changes
   before product integration;
4. deliver the regional vertical slice, then benchmark volume Focus/Gallery
   using existing resources before proposing any bulk contract.

Stop at Q17 for real normalization populations and parameters. Run focused
tests during each slice and `just check` before every coherent commit.
