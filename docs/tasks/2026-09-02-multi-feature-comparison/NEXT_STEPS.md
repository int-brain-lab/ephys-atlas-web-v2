# Next steps

As the current immediate priority, with the catalog/edition machinery,
scalar-presentation follow-ups, and documentation screenshots complete:

1. launch the workbench with `just comparison-ux-lab` and run the first
   task-based scientist review described in `README.md`;
2. record observations and accepted UX changes in this task record before
   product integration;
3. validate tile density, canonical grouping/order, click and pin behavior,
   and whether Gallery and Profile should remain separate modes;
4. after that review, deliver the regional vertical slice, then benchmark
   volume Focus/Gallery using existing resources before proposing any bulk
   contract.

Stop at Q17 for real normalization populations and parameters. Run focused
tests during each slice and `just check` before every coherent commit.
