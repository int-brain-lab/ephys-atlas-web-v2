# Selected optional 3-D anatomy asset

Status: frozen evidence; geometry/LOD fixed by D042 and immutable deployment
remains optional.

This record preserves the completed lab decision without making a fresh clone
depend on a local worktree or ignored generated bytes. It is not a volume
rendering contract.

## Authoritative source

- source: `https://ibl-brain-wide-map-public.s3.us-east-1.amazonaws.com/atlas/meshes.glb`
- bytes: `96,622,012`
- SHA-256:
  `487a72172249acd4dba5b40c392fa8eb065b09bc8638f3195163c4cbf8f569db`
- frozen donor commit:
  `ba1e2d129753bdc459bca7b23fa896f41ee13536`

The GLB is the geometry authority. Do not infer required surfaces from the 2-D
projection corpus and do not replace GLB surfaces with annotation-derived
geometry.

## Selected compiled-full evidence

- donor resource: `source.eamh.gz`
- bytes: `4,958,039`
- SHA-256:
  `658d68d81619ef83f7dbd6b032533ecd751fb52d3e7dd734dc90b1086b95baaa`
- triangles: `989,811` (`100%` after selected scope/cut/caps)
- signed surfaces: `1,130`
- positive source objects: `566`
- encoding: 14-bit positions, 8-bit octahedral normals, meshopt, deterministic
  gzip
- selected LOD policy: compiled-full only; no upgrade
- geometry policy: no smoothing, no triangle decimation, no voxel-derived
  replacement surfaces

The compiled resource retains every triangle remaining after the reviewed
deepest-active grey-matter selection, exact ML=0 bilateral cut, and planar
midline caps. "Full" refers to that selected compiled inventory, not the raw
96.6 MB GLB container or regions outside the selected scope.

The exact donor bytes remain ignored evidence rather than a Git dependency.
Current `main` owns the schema-v1 renderer and test fixture. A future immutable
deployment may repackage the selected geometry into EAM3, but must prove the
same source inventory and triangle topology and must not reopen geometry or LOD
selection. No 3-D asset is needed to build or test the linked 2-D production
volume path.
